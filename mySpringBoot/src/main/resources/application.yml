server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fuwuwaibao?serverTimezone=GMT%2b8
    username: root
    password: hy1999wjy2003
mybatis:
  mapper-locations: classpath:mapper/*.xml #扫描所有的mybatis?xml文件
  #configuration:
   # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus:
  configuration:
    log-impl:  org.apache.ibatis.logging.stdout.StdOutImpl

files:
   upload:
     path: D:/FuWuWaiBao/files/
